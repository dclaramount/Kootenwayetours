apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kootenwayetours

resources:
  - namespace.yaml
  - configmap.yaml
  - postgres-statefulset.yaml
  - postgres-service.yaml
  - app-pvc-uploads.yaml
  - app-deployment.yaml
  - app-service.yaml
  - ingress.yaml

# secret.example.yaml is intentionally NOT listed here — it's a template.
# Create the real Secret with `kubectl create secret`, not by applying a
# file. See README.md in this directory.
