apiVersion: v1
kind: Service
metadata:
  name: app
  namespace: kootenwayetours
  labels:
    app: kootenwayetours
spec:
  selector:
    app: kootenwayetours
  ports:
    - name: http
      port: 80
      targetPort: http
