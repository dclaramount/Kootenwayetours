# Sample images

These SVGs are hand-drawn placeholder illustrations, not real photography.
They exist purely so seeded content has something to display while `npm run
dev` is running. Replace them by uploading real photos through the admin
media library once it's built (see `planning-design/`) — the tour, blog, and
gallery seed data can then be repointed at the real assets.
